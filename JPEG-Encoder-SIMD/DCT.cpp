#include "stdafx.h"
#include "DCT.h"

float * DCT::directDCT(float * values)
{
	//float 
	//for (int i = 0; i < 8; i++) {
	//	for (int j = 0; j < 8; j++) {
	//		float ci = i == 0 ? 1 / sqrt(2) : 1;
	//		float cj = j == 0 ? 1 / sqrt(2) : 1;

	//		M[i,j] = 2/8*
	//	}
	//}
	return nullptr;
}
